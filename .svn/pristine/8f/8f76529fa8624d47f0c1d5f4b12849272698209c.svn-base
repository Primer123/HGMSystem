(function(window, $, undefined){
    var changeRoomService = {
        attachEvent: function attachEvent() {
        	 /** Begin of createRecordBtn **/
            $('.waiterChangeRoom').on("click", function(){
                var $createModel = $('#RecordCheckinModal');
                $createModel.modal({
                    backdrop: 'static'
                });
            });
        },
        init: function init() {
            this.attachEvent();
        }
    }

    changeRoomService.init();

}(window || this, jQuery, undefined));



function roomsearch() {
	var changerooom_form = document.all.changeroom_form;
//	checkin_form.action = "checkinFormSearch";
	changeroom_form.submit();
}
