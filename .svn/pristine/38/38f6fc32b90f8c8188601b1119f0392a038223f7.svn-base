(function(window, $, undefined){
    var recordCheckinService = {
        attachEvent: function attachEvent() {
        	 /** Begin of createRecordBtn **/
            $('.waiterCheckin').on("click", function(){
                var $createModel = $('#RecordCheckinModal');
                $createModel.modal({
                    backdrop: 'static'
                });
            });
        },
        init: function init() {
            this.attachEvent();
        }
    }

   recordCheckinService.init();

}(window || this, jQuery, undefined));