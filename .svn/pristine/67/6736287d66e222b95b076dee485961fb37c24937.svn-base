(function(window, $, undefined){
    var goodManageService = {
        attachEvent: function attachEvent() {
        	 /** Begin of createRecordBtn **/
            $('.adminAdd').on("click", function(){
                var $createModel = $('#AddGoodModal');
                $createModel.modal({
                    backdrop: 'static'
                });
            });
        },
        init: function init() {
            this.attachEvent();
        }
    }

    goodManageService.init();

}(window || this, jQuery, undefined));
